openapi: 3.1.0
info:
  title: MindfulPenPal Generation API
  version: 1.1.0
  description: Internal API consumed only by the signed Next.js BFF.
security:
  - viewerContext: []
paths:
  /v1/generation/workflows:
    get:
      operationId: listGenerationWorkflows
      parameters:
        - in: query
          name: mode
          required: false
          schema: { type: string, enum: [workflow, api] }
        - in: query
          name: mediaType
          required: false
          schema: { $ref: "#/components/schemas/MediaType" }
      responses:
        "200":
          description: Enabled creation methods with an eligible provider binding, split by workflow/api mode.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items: { type: array, items: { $ref: "#/components/schemas/Workflow" } }
  /v1/generations:
    get:
      operationId: listGenerations
      parameters:
        - in: query
          name: cursor
          required: false
          schema: { type: string }
        - in: query
          name: limit
          required: false
          schema: { type: integer, minimum: 1, maximum: 50 }
        - in: query
          name: status
          required: false
          schema: { $ref: "#/components/schemas/GenerationStatus" }
      responses:
        "200":
          description: Viewer-scoped recent generation tasks with an optional signed next cursor.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GenerationPage" }
        "401": { $ref: "#/components/responses/Error" }
    post:
      operationId: createGeneration
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, pattern: "^[a-zA-Z0-9][a-zA-Z0-9_-]{0,127}$" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateGeneration" }
      responses:
        "202":
          description: Durable task, credit reservation, outbox and audit were created atomically.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Generation" }
        "401": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
  /v1/generations/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, maxLength: 128 }
    get:
      operationId: getGeneration
      responses:
        "200":
          description: Owner/admin scoped task state from PostgreSQL.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Generation" }
        "404": { $ref: "#/components/responses/Error" }
    delete:
      operationId: cancelGeneration
      responses:
        "200":
          description: Idempotent cooperative cancellation result.
          content:
            application/json:
              schema:
                type: object
                required: [generation, accepted]
                properties:
                  generation: { $ref: "#/components/schemas/Generation" }
                  accepted: { type: boolean }
components:
  securitySchemes:
    viewerContext: { type: apiKey, in: header, name: X-Mavis-User-Context }
  responses:
    Error:
      description: Sanitized error
      content:
        application/json:
          schema:
            type: object
            required: [error]
            properties:
              error:
                type: object
                required: [code, message]
                properties: { code: { type: string }, message: { type: string } }
  schemas:
    Workflow:
      type: object
      required: [slug, name, description, category, mode, mediaType, defaults, countRange, sizes, durations, creditCost]
      properties:
        slug: { type: string }
        name: { type: string }
        description: { type: string }
        category: { type: string }
        mode: { enum: [workflow, api] }
        mediaType: { $ref: "#/components/schemas/MediaType" }
        defaults:
          type: object
          required: [width, height, count, visibility, promptVisibility]
          properties:
            width: { type: integer }
            height: { type: integer }
            count: { type: integer }
            visibility: { enum: [public, private] }
            promptVisibility: { enum: [public, hidden] }
            durationSeconds: { type: integer, minimum: 1, maximum: 300 }
        countRange:
          type: object
          required: [min, max]
          properties:
            min: { type: integer }
            max: { type: integer }
        sizes:
          type: array
          items:
            type: object
            required: [width, height]
            properties:
              width: { type: integer }
              height: { type: integer }
        durations:
          type: array
          items: { type: integer, minimum: 1, maximum: 300 }
        creditCost: { type: string, pattern: "^\\d+\\.\\d{4}$" }
    CreateGeneration:
      type: object
      additionalProperties: false
      required: [workflowSlug, prompt, negativePrompt, width, height, count, visibility, promptVisibility, parameters]
      properties:
        workflowSlug: { type: string, maxLength: 128 }
        prompt: { type: string, minLength: 1, maxLength: 8000 }
        negativePrompt: { type: string, maxLength: 8000 }
        width: { type: integer, minimum: 64, maximum: 8192 }
        height: { type: integer, minimum: 64, maximum: 8192 }
        count: { type: integer, minimum: 1, maximum: 8 }
        visibility: { enum: [public, private] }
        promptVisibility: { enum: [public, hidden] }
        parameters: { type: object }
        inputImages:
          type: array
          maxItems: 3
          description: 参考图 data URL（PNG/JPEG/WebP，单张不超过 3MB），用于单图/多图参考视频工作流。
          items:
            type: object
            additionalProperties: false
            required: [name, data]
            properties:
              name: { type: string, maxLength: 128 }
              data: { type: string, description: "data:image/png;base64,..." }
    Generation:
      type: object
      required: [id, status, workflowSlug, workflowName, mediaType, prompt, negativePrompt, width, height, count, visibility, promptVisibility, creditsReserved, creditsCharged, cancelRequested, createdAt, updatedAt, images, outputs]
      properties:
        id: { type: string }
        status: { $ref: "#/components/schemas/GenerationStatus" }
        workflowSlug: { type: string }
        workflowName: { type: string }
        mediaType: { $ref: "#/components/schemas/MediaType" }
        prompt: { type: string }
        negativePrompt: { type: string }
        width: { type: integer }
        height: { type: integer }
        count: { type: integer }
        visibility: { enum: [public, private] }
        promptVisibility: { enum: [public, hidden] }
        creditsReserved: { type: string }
        creditsCharged: { type: string }
        cancelRequested: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        finishedAt: { type: string, format: date-time }
        images:
          type: array
          items:
            type: object
            required: [id, slug]
            properties: { id: { type: string }, slug: { type: string } }
        outputs:
          type: array
          description: Owner-scoped durable non-Gallery outputs. Image Gallery links remain in images for backward compatibility.
          items:
            type: object
            required: [id, mediaType, url, mimeType, width, height]
            properties:
              id: { type: string }
              mediaType: { $ref: "#/components/schemas/MediaType" }
              url: { type: string, format: uri }
              mimeType: { type: string }
              width: { type: integer, minimum: 1 }
              height: { type: integer, minimum: 1 }
              durationSeconds: { type: number, exclusiveMinimum: 0, maximum: 300 }
    MediaType:
      type: string
      enum: [image, video]
    GenerationStatus:
      type: string
      enum: [pending, running, completed, failed, cancelled]
    GenerationPage:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Generation" }
        nextCursor: { type: string }
